# Squid configuration with Kerberos and LDAP authentication
# Basic configuration

# PID file location
pid_filename /var/run/squid/squid.pid

# HTTP port
http_port 3128

# HTTPS port for SSL bump (закомментировано, т.к. требует SSL сертификаты)
# https_port 3129 cert=/etc/squid/ssl/squid.crt key=/etc/squid/ssl/squid.key

# Access control lists
acl localnet src 10.0.0.0/8     # RFC1918 possible internal network
acl localnet src 172.16.0.0/12  # RFC1918 possible internal network  
acl localnet src 192.168.0.0/16 # RFC1918 possible internal network
acl localnet src fc00::/7        # RFC 4193 local private network range
acl localnet src fe80::/10       # RFC 4291 link-local (directly plugged) machines

# Docker network ACLs for SqStat access
acl localhost src 127.0.0.1/32  # localhost for cachemgr access
acl docker_net src 192.168.64.0/20  # Docker network subnet
acl apache_analyzer src 192.168.64.3/32  # squid-apache-analyzer container IP

acl SSL_ports port 443
acl Safe_ports port 80          # http
acl Safe_ports port 21          # ftp
acl Safe_ports port 443         # https
acl Safe_ports port 70          # gopher
acl Safe_ports port 210         # wais
acl Safe_ports port 1025-65535  # unregistered ports
acl Safe_ports port 280         # http-mgmt
acl Safe_ports port 488         # gss-http
acl Safe_ports port 591         # filemaker
acl Safe_ports port 777         # multiling http
acl CONNECT method CONNECT

# Cache Manager access control for SqStat
# Разрешаем cachemgr только для localhost и docker сети
http_access allow manager localhost
http_access allow manager docker_net
http_access deny manager

# Deny requests to certain unsafe ports
http_access deny !Safe_ports

# Deny CONNECT to other than secure SSL ports
http_access deny CONNECT !SSL_ports

# Kerberos authentication configuration (закомментировано для базовой работы)
# auth_param negotiate program /usr/lib/squid/negotiate_kerberos_auth -s GSS_C_NO_NAME
# auth_param negotiate children 10
# auth_param negotiate keep_alive on
# acl authenticated proxy_auth REQUIRED

# LDAP authentication configuration (закомментировано для базовой работы)
# auth_param basic program /usr/lib/squid/basic_ldap_auth -R -b "dc=example,dc=com" -D "cn=squid,ou=services,dc=example,dc=com" -w "password" -f sAMAccountName=%s -h ldap.example.com
# auth_param basic children 5
# auth_param basic realm Squid proxy-caching web server
# auth_param basic credentialsttl 2 hours
# acl ldap_users proxy_auth REQUIRED

# Access rules
http_access allow localnet
http_access allow localhost
http_access allow docker_net

# And finally deny all other access to this proxy
http_access deny all

# Cache directory
cache_dir ufs /var/spool/squid 100 16 256

# Leave coredumps in the first cache dir
coredump_dir /var/spool/squid

# Logging
access_log /var/log/squid/access.log squid
cache_log /var/log/squid/cache.log

# Cache Manager password for SqStat access
# Пароль для доступа к cachemgr интерфейсу (используется SqStat)
cachemgr_passwd squid_stats all

# Refresh patterns
refresh_pattern ^ftp:           1440    20%     10080
refresh_pattern ^gopher:        1440    0%      1440
refresh_pattern -i (/cgi-bin/|\?) 0     0%      0
refresh_pattern .               0       20%     4320
